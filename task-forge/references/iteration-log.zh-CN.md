# 迭代记录

每次状态变更后追加一段，不得改写已有记录。

## 第 N 次迭代
- 对应契约 SHA-256：
- 目标验收项：
- 操作前退出条件检查：
- 预期状态变化：
- 已执行动作：
- 变更的文件或系统：
- 验证命令或方法：
- 结果：`PASS` 或 `FAIL`
- 证据：
- 受保护验证器与不变量检查：
- 状态指纹：
- 剩余差距：
- 已用及剩余预算：
- 下一步：`CONTINUE`、`DONE`、`NEEDS_APPROVAL` 或 `BLOCKED`
